# Spectrum data

Put oscilloscope CSV exports here (one trace per file).

The notebooks read `*.CSV` and `*.csv` from this folder. This directory is not synced via git — copy or share spectra separately with your group.
